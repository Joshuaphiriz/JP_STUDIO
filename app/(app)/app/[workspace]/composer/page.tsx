import { ComingSoon } from "@/components/shell/coming-soon";

export const metadata = { title: "Composer" };

export default function ComposerPage() {
  return (
    <ComingSoon title="Composer" phase="Phase 1">
      Rich composing with per-platform overrides, media, first comment, and live
      previews arrives once account connections land.
    </ComingSoon>
  );
}
